# Amazon Control UI v3 美学方案

## 参考结论

- SellerApp：深色侧栏 + 高密度数据面板，信息优先，行动按钮集中在右侧。
- Perpetua：单一强强调色，大量留白，数据用等宽数字展示，降低装饰噪音。
- Helium 10 / Jungle Scout：深色导航 + 浅色工作区，表格密集，橙色/青绿作为行动色。
- Amazon Seller Central：白底、浅灰工作区、蓝色链接，表单克制，适合高频操作。

本项目定位是“聊天主控 + 模块工作台”的本地 AI 运营平台，因此视觉语言采用高密度运营控制台，而不是营销官网。

## 设计令牌

| 角色 | 色值 | 用途 |
|------|------|------|
| Ink | `#17241f` | 正文、标题 |
| Forest | `#10241d` | 侧栏底色 |
| Signal Green | `#0e7c60` | 主操作、成功 |
| Teal | `#177f79` | 数据强调 |
| Amber | `#d69b2e` | P1 提醒 |
| Red | `#c14b3f` | 风险、失败 |
| Blue | `#3f6fae` | 信息、运行中 |
| Canvas | `#f2f6f4` | 页面底色 |
| Line | `#dce6e1` | 分隔线 |

- 圆角统一为 8px，弹层和会话卡片不超过 10px。
- 数字与指标使用等宽字体 `Cascadia Mono`，便于纵向扫描。
- 主字体使用 `Microsoft YaHei` / `Segoe UI Variable`，字距保持 0。
- 背景使用浅色网格纹理，不用渐变光斑或装饰性圆形。

## 布局原则

1. 侧栏固定承载品牌、运行状态、模块导航和快捷入口。
2. 顶部上下文栏始终显示项目、站点、数据源、接口和同步状态。
3. 模块页统一为“输入区 + 预览/结果区 + 历史/导出区”，不把导航入口混进内容区。
4. 聊天作为主入口，右侧展示自动导入的数据、素材资产和结果摘要。
5. 状态色只用于真实状态，不用于装饰；高风险动作必须进入审批状态。

## 已落地

- `web-dashboard/styles.css` 已追加 v3 视觉覆盖：色板、背景网格、圆角、按钮、聊天、卡片、表格、滚动条。
- 保留现有 HTML 结构和交互逻辑，不改变功能行为。

## 后续可做

- 设计令牌已抽成 `web-dashboard/design-tokens.json`，供未来主题切换和多模型皮肤使用。
- 增加深色主题变量，默认跟随系统。
- 把模块页从通用两栏升级为各模块专属工作区，同时沿用同一套视觉语言。

## Aether Deck 动能层

- 引入本地内置的 anime.js v4 运行库，离线可用，不依赖 CDN。
- 启动序列使用 spring、stagger 和 timeline 组合呈现。
- Agent Kinetic Dock 支持水平拖拽、弹簧回位、节点旋转和网络脉冲联动。
- 全局 AI 核心支持双轴拖拽，释放后弹簧归位，并实时牵引指挥网络。
- 模块面板、指标卡、导航按钮和操作按钮增加磁吸和分层入场动效。
- 所有动能组件在 `prefers-reduced-motion` 下自动降级。

## Reevo 同构视觉方向

- 参考 Godly 收录的 Reevo.ai，提取白色主画布、超大黑体标题、2px 圆角、细边界和黑色主按钮。
- 信息色使用橙 `#f96a1a`、绿 `#49b585`、蓝 `#358bf0`、青 `#4ab7ca`、黄 `#f8c647` 和粉 `#e36fb6`。
- 当前默认模式为 `REEVO MODE`，可以在顶部切换回原有 `OPS MODE`。
- Amazon Control 使用自己的品牌、文案、结构和资产，不复用对方代码、图片或原始文案。

## Particle Interaction Layer

- 全屏 Canvas 粒子场支持鼠标斥力、点击冲击波、网络连线和节点脉冲。
- 拖拽 Agent 或 AI 核心会实时改变粒子速度、网络重心和事件流。
- 页面滚动时，指标卡、产物卡和历史任务使用 IntersectionObserver 联动入场。
- 桌面端提供坐标十字准星，移动端和 reduced-motion 环境自动关闭。
- 顶部 `FIELD` 遥测显示当前活跃粒子数量。

## Browser Workspace Shell

- 标签栏固定在页面最顶部，第二行统一承载前进后退、刷新、新标签、地址栏、运行状态和模式切换。
- 模块点击默认打开或聚焦标签页，不再直接替换当前页面内容。
- 新标签页提供主模块、专家工作流、最近任务和命令提示。
- 地址栏支持模块名、`amazon://`、ASIN、文件路径、链接和自然语言。
- 标签状态、历史、滚动位置和当前任务写入 `localStorage`，重启后可恢复。
- 支持 `Ctrl+T`、`Ctrl+W`、`Ctrl+L`、`Ctrl+Tab`、`Alt+Left/Right`。
- 左侧导航收束为 `78px` 图标轨道，模块以短码呈现，主内容区获得更大空间。

## Asset Drag Import

- 素材库和聊天页都提供 `DROP ASSETS` 拖拽区域。
- Electron 环境优先读取原始本地路径，不复制网页 Base64。
- 网页环境将文件转换为 Base64 上传给本地服务，并保存到素材目录。
- 单个网页拖拽文件限制为 `8 MB`，支持图片、视频、CSV、JSON、压缩包和链接。

## Human-readable Result Reports

- 结果页优先展示结论、关键指标、建议动作和风险，不默认露出 JSON。
- 广告、利润、库存、Listing、图片和视频结果使用各自的人类可读模板。
- 数据表默认只展示前 12 行，原始数据折叠在“查看原始数据”中。
- 聊天回复统一使用中文，例如“已导入 1 个数据源”“优先动作”。
